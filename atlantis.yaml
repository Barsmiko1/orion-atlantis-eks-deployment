version: 3
automerge: true
projects:
- name: default
  dir: .
  workspace: default
  terraform_version: v1.5.0
  autoplan:
    when_modified: ["*.tf", "*.tfvars"]
    enabled: true
  apply_requirements: ["approved"]
  workflow: custom
